# Disables the CRA ESLint plugin during the production build.
# Vercel sets CI=true which turns ESLint warnings into hard errors — this prevents that.
# ESLint still runs normally in local development.
DISABLE_ESLINT_PLUGIN=true
GENERATE_SOURCEMAP=false
